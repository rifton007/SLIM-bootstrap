{% extends '/Layouts/offline.twig' %}
{% block content %}
    <div class="row">
        <div class="col-12">
            <h1 class="mb-5 text-center">{{ company.name }}</h1>
        </div>
    </div>
    <div class="row justify-content-center">
        <div class="col-12 text-center col-lg-3 col-sm-8">
            <img src="img/icon_fr.png" alt="language_fr" width="70" class="mb-4">
            <p class="text-justify-center">
                Ce site internet est en cours de maintenance et sera en ligne tout prochainement.
                <strong>{{ company.name }}</strong> reste à votre entière disposition et reste joignables aux
                coordonnées indiquées ci-dessous.
                Nous vous prions de nous excuser pour ce désagrément.
            </p>
        </div>
        <div class="col-12 text-center col-lg-3 col-sm-8">
            <img src="img/icon_nl.png" alt="language_nl" width="70" class="mb-4">
            <p class="text-justify-center">
                Deze website ondergaat onderhoud en zal binnenkort online zijn. <strong>{{ company.name }}</strong> blijft tot uw beschikking en wij
                blijven bereikbaar op de hieronder aangegeven coördinaten. Onze excuses voor dit ongemak.
            </p>
        </div>
        <div class="col-12 text-center col-lg-3 col-sm-8">
            <img src="img/icon_en.png" alt="language_en" width="70" class="mb-4">
            <p class="text-justify-center">
                This website is undergoing maintenance and will be online shortly. <strong>{{ company.name }}</strong> remains at your disposal and we
                remain reachable at the coordinates indicated below. Please excuse us for this inconvenience.
            </p>
        </div>
    </div>
    <div class="row justify-content-center mt-5 ">
        <div class="col-12 col-lg-5 col-sm-12 mb-3 ">
            <div class="card text-center h-100">
                <div class="card-header">
                    Contactez-nous
                </div>
                <div class="card-body">
                    <h5 class="mb-3">{{ company.name }}</h5>
                    <p>
                        <i class="fa fa-map-marker"></i>&ensp;{{ company.address }}
                        <br>{{ company.postcode }} {{ company.city }}<br>
                        <i class="fa fa-phone"></i>&ensp;<a href="tel:{{ company.phone }}">{{ company.phone }}</a>
                        <br>
                        <i class="fa fa-at"></i>&ensp;<a href="mailto:{{ company.email }}">{{ company.email }}</a>
                    </p>
                </div>
            </div>
        </div>
    </div>
{% endblock %}